apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-service
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  selector:
    matchLabels:
      app: products-service
  template:
    metadata:
      labels:
        app: products-service
    spec:
      containers:
        - name: products-service
          image: maurig13/devops-grisi-reyes:products-service-dev
          ports:
            - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: products-service
  labels:
    name: products-service
spec:
  selector:
    name: products-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30036
      protocol: TCP
  type: LoadBalancer
